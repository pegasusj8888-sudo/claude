# 트랜스폰더 DB 작업 규칙 (모든 브랜드 엑셀 공통)

- 컬럼: 맨 앞 '브랜드'(모든 행에 브랜드명) + 기아_트랜스폰더_칩코드_DBnew.xlsx 구조 + '키종류'(칩코드와 키블레이드(부품번호) 사이). 브랜드 컬럼은 research/add_brand.py로 추가(build_xlsx.py 저장 뒤 자동 호출). 모델별 연식당 1행, 2000년식부터, 연식별 웹 검색 근거.
- 모든 엑셀의 행은 1줄로 통일: 셀 줄바꿈(wrap_text) 끄고 행 높이 기본값 유지. 데이터 시트·안내 시트 모두 적용.
- 키종류: 막대키, 폴딩키, 스마트키, 카드키 중 해당 연식에 있는 것을 모두 쉼표로 (예: 막대키,폴딩키).
- 한국 기준: 국내 사양(433/434/447MHz) 키만 기재. 315MHz(미국)·868MHz(유럽) 등 해외 주파수 키 부품번호는 넣지 않음.
- 비고: 주황색·빨간색 칸의 이유만 적음. 색 없는 행은 비고 공란. 출시/단종 등 부가 설명은 '안내' 시트에.
- 색: 주황(FFD599)=추정·자료 상충, 빨강(FFC7CE)=확인 불가. 칩코드·이모빌라이저·비고 칸에 칠함. 전환기는 전·후 값을 함께 적고 색 없음.
- 키블레이드 부품번호는 찾은 경우만, 없으면 공란.
- 원본 목록 연식이 실제 국내 판매와 다르면 실제 기준으로 작성.
- 표기 통일(칩코드·부품번호·키웨이·이모빌라이저 칸): 여러 값은 ", "로만 구분('/', '·', '또는', 범위·접미사 줄임 금지). ( ) 안은 부품번호만 — 칩코드는 ID코드(칩 부품번호) 예: ID49(PCF7953), 모름이면 ID49. [ ] 안은 적용 조건만(키 종류·버튼 수·옵션·생산 시기·차종 코드·추정·일부 자료·자료 상충). Hitag2 등 방식 이름·설명·판매처·주파수는 칸에 쓰지 않음(안내 시트 ID 코드 표). 부품번호 칸은 부품번호만(FCC ID는 'FCC ', 한국 전파인증은 'KC ' 접두, 호환품은 [애프터마켓]). 변환·검사: research/notation.py (--check로 위반 확인, build_xlsx.py 저장 뒤 자동 적용).
- 후처리 공통 로직: research/finalize.py (해외 주파수 제거·키종류·비고 사유).
